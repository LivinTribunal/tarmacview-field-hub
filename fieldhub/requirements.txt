# FastAPI + server
fastapi==0.136.1
uvicorn[standard]==0.46.0

# Validation / config
pydantic==2.13.3
pydantic-settings==2.14.0

# Multipart form parsing - internal wayline register upload
python-multipart==0.0.27

# HTTP client - object-store health probe
httpx==0.28.1

# S3-compatible object store client - wayline KMZ storage, presigned urls,
# sts assume-role + bucket ensure
minio==7.2.20

# Device registry persistence
sqlalchemy==2.0.49
psycopg[binary]==3.3.4

# MQTT listener (emqx)
aiomqtt==2.5.1

# x-auth-token JWTs
python-jose[cryptography]==3.5.0

# Testing
pytest==9.0.3
pytest-asyncio==1.3.0

# Dev
ruff==0.15.12
